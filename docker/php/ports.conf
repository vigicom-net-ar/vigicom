# Reemplaza el ports.conf default de Debian/Apache.
# Apache escucha un puerto distinto por cada vhost de vigicom, mismos numeros
# adentro del contenedor y afuera (compose los mapea 1:1) y mismos numeros
# entre dev y prod (en prod nginx proxea cada subdominio al puerto que toca).
Listen 8090
Listen 8104
Listen 8105
Listen 8106
Listen 8107
Listen 8108
